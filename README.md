<div align="center">

# ChatGL-MC

[![Minecraft](https://img.shields.io/badge/Minecraft-1.21.4%2B-5E9C3F?style=for-the-badge&logo=minecraft&logoColor=white)](https://www.minecraft.net/)
[![Paper](https://img.shields.io/badge/Paper-API-blue?style=for-the-badge)](https://papermc.io/)
[![Java](https://img.shields.io/badge/Java-21-ED8B00?style=for-the-badge&logo=openjdk&logoColor=white)](https://openjdk.org/)
[![License](https://img.shields.io/badge/License-Apache%202.0-lightgrey?style=for-the-badge)](./LICENSE)

Простой и надежный плагин для **разделения чата на локальный и глобальный**.

</div>

---

## ✨ Возможности

- **Локальный чат** по умолчанию (сообщение видят игроки в радиусе `50` блоков).
- **Глобальный чат** через префикс `!` (сообщение видит весь сервер).
- Сообщение игроку, если его **никто не услышал** в локальном чате.
- Гибкая настройка через `config.yml`:
  - радиус локального чата;
  - префикс глобального чата;
  - форматы сообщений;
  - текст уведомления о том, что рядом никого нет.

---

## 🧠 Как это работает

### Логика маршрутизации сообщений

1. Игрок отправляет сообщение в чат.
2. Плагин перехватывает событие `AsyncChatEvent`.
3. Если сообщение начинается с `!`, оно отправляется в **глобальный** канал.
4. Если `!` нет — сообщение отправляется в **локальный** канал в пределах радиуса.
5. Если в локальном радиусе нет других игроков, отправителю показывается:
   > `Вас никто не услышал, нет игроков в радиусе 50м.`

### Схема работы

```mermaid
flowchart TD
    A[Игрок отправил сообщение] --> B{Начинается с !?}
    B -->|Да| C[Глобальный чат]
    C --> D[Отправить всем онлайн игрокам]

    B -->|Нет| E[Локальный чат]
    E --> F[Найти игроков в том же мире и радиусе]
    F --> G[Отправить локальное сообщение адресатам]
    G --> H{Есть другие игроки
    кроме отправителя?}
    H -->|Нет| I[Показать: Никто не услышал]
    H -->|Да| J[Завершить]
```

---

## 📦 Установка

1. Скачайте/соберите `.jar` плагина.
2. Поместите файл в папку `plugins/` вашего Paper-сервера.
3. Перезапустите сервер.
4. При первом запуске будет создан `plugins/ChatGL/config.yml`.

---

## 🔌 Совместимость с PlaceholderAPI

Да, плагин совместим с **PlaceholderAPI**.

- Если PlaceholderAPI установлен на сервере, ChatGL автоматически подхватывает его и обрабатывает сторонние плейсхолдеры.
- Если PlaceholderAPI не установлен, плагин продолжает работать без ошибок (просто без сторонних плейсхолдеров).

Поддержка работает в:
- `chat.formats.local`
- `chat.formats.global`
- `chat.messages.no-listeners`

Пример формата с плейсхолдерами из других плагинов:

```yaml
chat:
  formats:
    global: "<gold>[G]</gold> %vault_prefix% <white>%player%</white>: <white>%message%</white>"
```

> Важно: конкретные плейсхолдеры (например `%vault_prefix%`) появятся только если установлен соответствующий плагин и его expansion в PlaceholderAPI.

---

## ⚙️ Конфигурация

```yaml
chat:
  local-radius: 50
  global-prefix: "!"

  formats:
    local: "<gray>[L]</gray> <white>%player%</white>: <yellow>%message%</yellow>"
    global: "<gold>[G]</gold> <white>%player%</white>: <white>%message%</white>"

  messages:
    no-listeners: "<red>Вас никто не услышал, нет игроков в радиусе %radius%м.</red>"
```

### Пояснения

- `local-radius` — дальность локального чата в блоках.
- `global-prefix` — символ/строка для отправки в глобальный чат.
- `formats.local`, `formats.global` — шаблоны сообщений (MiniMessage).
- `messages.no-listeners` — уведомление, если рядом никого нет.

---

## 🛠️ Сборка из исходников

```bash
mvn clean package
```

Готовый артефакт будет в папке `target/`.

---


## 🧪 Проверка готового JAR

После сборки проверьте, что в архив действительно попал дескриптор плагина:

```bash
jar tf target/chatgl-mc-*.jar | rg "plugin.yml|paper-plugin.yml"
```

Если команда ничего не вывела — сервер не сможет загрузить плагин.

---

## 👤 Автор

**nestren**

---

## 📄 Лицензия

Проект распространяется по лицензии **Apache 2.0**. Подробности в [LICENSE](./LICENSE).
